@model SkyCommCoreMVC.Models.AirportTypes

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_AirportsLayout.cshtml";
}

<h1>Edit the Airport Type</h1>

<h4>Airport Types</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="AirportTypeId" />
            <div class="form-group">
                <label asp-for="AirportType" class="control-label"></label>
                <input asp-for="AirportType" class="form-control" />
                <span asp-validation-for="AirportType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
